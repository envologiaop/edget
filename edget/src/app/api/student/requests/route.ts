import { NextResponse } from 'next/server'\nimport { auth } from '@/auth'\nimport { prisma } from '@/lib/prisma'\n\nexport async function GET() {\n  const session = await auth()\n  if (!session || (session as any).role !== 'STUDENT') return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })\n  const items = await prisma.tutorRequest.findMany({ where: { studentId: (session.user as any).id }, include: { package: true, teacher: { include: { user: true } } }, orderBy: { createdAt: 'desc' } })\n  return NextResponse.json({ items })\n}\n